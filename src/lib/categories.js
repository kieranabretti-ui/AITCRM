// Mirrors netlify/functions/lib/safety.js's CATEGORIES — kept as a
// separate client-side copy since Netlify Functions code isn't
// importable from the browser bundle. Keep the two lists in sync.
export const CATEGORIES = [
  'Security', 'Microsoft 365', 'Endpoint', 'Backup', 'Network',
  'Account & Access', 'Hardware', 'Software', 'New User', 'User Change', 'Other',
]
